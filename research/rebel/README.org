#+TITLE: ReBeL — research stage (Kuhn/Leduc), validated before HUNL

Faithful implementation of ReBeL (Brown, Bakhtin, Lerer, Gong 2020,
arXiv:2007.13544) on the repo's exact-judge small games. Build target
=rebel=; see rebel.h for the faithfulness notes and the two documented
deviations (no policy-net warm start; unsafe test-time composition).

* Validation ladder (2026-07-04, this machine, CPU)

| test                                   | result                        |
|----------------------------------------+-------------------------------|
| kuhn exact (solver sanity, no leaves)  | expl 0.0056 → 0.00018 (T=1k)  |
| leduc exact-leaf CFR-D (decomposition) | expl ~0.10–0.12 (see below)   |
| leduc train, 20 epochs (full loop)     | expl 2.21 → 0.26–0.37, probe-MSE 0.062 → 0.012 |

* Findings worth remembering

1. *CFR-AVG is load-bearing.* Leaf beliefs from the CURRENT iterate
   (textbook CFR-D) diverge outright with a re-solved exact oracle
   (expl 1.6): continuation-equilibrium values jump discontinuously as
   the oscillating iterate moves the beliefs. Average-policy beliefs
   (the paper's CFR-AVG) converge. This reproduces the paper's stated
   motivation exactly.
2. *The unreached-subgame gap is real and grows with T.* As RM⁺
   sparsifies the trunk support, more round-boundary beliefs are
   zero-mass; their subgames are solved from uniform fallbacks and the
   best response farms them (expl rose 0.10→0.18 from T=1k→3k;
   REBEL_EVAL_SMOOTH=0.001 flattens it to 0.118→0.123). This is the
   classic unsafe-resolving cost — the safe-resolving gadget (Burch et
   al. 2014) is the known fix and the main open item.
3. *Per-query re-solved value oracles are WORSE than a trained net.*
   Sharpening the exact oracle (400→2000 inner iters) RAISED trunk
   exploitability (0.118→0.184): per-infostate values of a 2p0s
   subgame are not unique across equilibria, and sharper solves snap
   between selections as beliefs vary. A single trained v̂ is
   self-consistent by construction — the learned loop is expected to
   (and does) approach the composed floor. Don't use the exact mode as
   the quality bar; it's a mechanics smoke test.

* Stage 2 progress (2026-07-04)

Built and validated on this machine (target =rebel_hunl=):
- =hunl_kernels=: 1326-combo fold/showdown kernels, exact card removal
  (inclusion-exclusion + rank-sweep over TwoPlusTwo ranks).
  =rebel_hunl kernels=: max |fast − brute| = 5.8e-10 over 20 boards. PASS.
- =hunl_solver=: street-limited public-tree CFR+ where the betting tree is
  built by push/pop REPLAY of PokerEnvironment (pot math, raise sizes,
  all-in and legality byte-identical to the deployed abstraction).
  Internal range-BR exploitability as the judge:
  - river, sparse {f,c,pot,allin} (51 nodes):  1.33 → 0.20 → 0.033 bb (T=400)
  - river, full 14 actions (78,327 nodes):     2.66 → 0.54 → 0.28 bb (T=200)
- turn decomposition (StreetEnd chance leaves → per-river-card PBS values
  via ExactStreetOracle; all-in-one-card-early terminals enumerated exactly)
  — the Leduc round-1/round-2 pattern at HUNL scale.
  Cost note: each exact refresh ≈ |leaves|×48 river re-solves — the
  measured economic case for the value net (one batched forward instead).
- gap closers (2026-07-05), both exact judges from OUTSIDE the solver:
  - =check_terminals=: payoff model vs env ground truth on dealt hands —
    fold 0.000 / showdown 0.000 / allin 0.006 chips over 855 terminals,
    30 states. PASS. (Caught a REAL bug on first run: node paths were
    assigned post-recursion, truncating every path deeper than one action
    — invisible to the internal BR, which lives in the same tree. All
    pre-fix turn numbers were tainted; below are post-fix.)
    Also: env.terminal_utility is REALIZED utility — bogus for all-in
    terminals; the equity value only flows through the step reward.
  - =turn_true=: TRUE composed two-street exploitability
    (exploitability_composed — BR may deviate on the river, against the
    agent's re-solve composition): T=12: internal 2.90 / composed 2.99 bb;
    T=25: internal 1.29 / composed 1.38 bb. Composed ≥ internal as theory
    requires; unsafe-resolving gap ~0.09 bb at this root.
  The trainer's probe now uses the composed judge; value targets are in
  POT units (paper normalization).
- trainer observability + GPU (2026-07-05): =heldout= = masked MSE on each
  epoch's FRESH samples BEFORE training on them (in-replay MSE is
  memorization at small scale — the old probe read 0.000000); per-epoch
  phase timings; the net lives on CUDA when available and leaf queries are
  ONE batched [48, D] forward per leaf. Expected GPU profile: utilization
  stays LOW — the CFR walks are CPU-bound per-combo recursion; the GPU
  serves leaf forwards + training. Parallel episodes / flat-tensor solver
  are the next scaling steps if CPU saturates.
- sample-rate fix (2026-07-05): the box run showed the diagnosis clearly —
  heldout flat ~0.3 through 4.5k samples, vloss rising: NOT a bug, sample
  starvation (1 target per 120-iteration turn solve; this regression class
  needs 1e5-1e7 rows — DeepStack used ~1M river situations). Fixes:
  (1) harvest: each turn solve also exact-solves `harvest` random
  (leaf,card) PBSs from its own final-average query distribution (~9
  targets/episode); (2) =train_river= mode (DeepStack recipe): direct
  river-situation sampling, no turn solves — ~28 targets/s on 8 laptop
  cores, first genuine learning curve (heldout 0.19→0.10 in 3 epochs).
  Net checkpointing added (rebel_value.pt, load-if-exists + save per
  epoch) for the two-phase workflow: big train_river pretrain → train_turn
  fine-tune on the on-distribution data. seed=0 now draws a random seed
  (printed) — fixed-seed resumes replayed identical "fresh" data.
- range-distribution fix (2026-07-05): box runs proved the river net
  learned its OWN distribution well (heldout 0.19→0.0044 over 250k iid
  samples) but was ~30× worse on the SOLVER's distribution (turn-harvest
  heldout ~0.15; probe-turn-expl stuck ~10 bb vs ~4.8 exact ref): iid
  noise ranges ≠ the sparse/correlated ranges CFR generates — the
  DeepStack problem, fixed the DeepStack way: 70% of direct-sampled
  ranges now come from recursive stick-breaking over random combo
  partitions; street-root prefixes also use varied raise sizes {4,7,10}
  for pot coverage. Retrain from a FRESH ckpt (the old net's
  distribution is obsolete); success = turn-mode heldout approaching
  river-mode heldout and probe-turn-expl(net) finally tracking toward
  the exact-leaf reference.
- GPU batch river solver (2026-07-05, hunl_gpu.*): B subgames in LOCKSTEP
  as [B, A, 1326] tensors — matmul fold masses, per-row sorted-cumsum
  showdown sweep with exact card removal, shared topology grouped by
  signature, per-spec pots/boards/ranges. =gpu_check= equivalence vs the
  fp64 CPU solver: f64 max 6.9e-5 of pot (exact); f32 max 5.5e-3 (pure
  accumulation drift over 200 iterations — negligible vs net error).
  =REBEL_GPU_BATCH=512= routes train_river target solving through it
  (CPU workers build specs, device solves). CUDA-only in practice: on
  CPU-torch the tensor kernels are ~100x slower than the worker path
  (warned at runtime). Board-cached =RiverEval= also added (~1.75x on all
  CPU river solves). Next perf steps if needed: overlap spec-building
  with device solves; batch bigger; fuse showdown gathers.
- CPU parallel self-play (2026-07-05): episodes fan out over a worker pool
  (own env + RNG per worker, work-stealing counter, net read-only; torch
  intra-op pinned to 1; HandRanks pre-warmed before spawn). REBEL_THREADS
  overrides (default hardware_concurrency). M3/8 workers: ~4.3× epoch
  throughput. Still sequential: the one-time exact reference probe and the
  every-5-epoch composed probe (parallelizing exploitability_composed's
  per-(leaf,card) river solves is the next easy win if probes annoy).

- river-pretrain verdict + screening infra (2026-07-06): 200-epoch
  =train_river= (4k targets/epoch, ~800k sampled, replay cap 300k):
  heldout 2.5e-2 → ~1.05e-2 pot-units MSE, FLAT from ~epoch 120 while
  unique data nearly doubled — capacity/feature-bound, not data-bound
  (vloss/heldout gap only ~1.8×; train 2s vs sampling 36s per epoch, so
  net growth is free). Probe gotcha found while instrumenting: the shared
  probe env's deal RNG advances across resets, so every probe solved a
  DIFFERENT board (the exact-leaf "reference" too) — the probe column's
  scatter was situation variance. =probe_turn_expl= now builds its env
  from the situation seed (truly fixed) and =REBEL_PROBE_K= averages K
  situations with matching exact references (0 = skip probes).
  Sample persistence: =REBEL_DATA_OUT= appends every fresh target to a
  dataset file (targets are net-independent exact solves — reusable
  forever; ~24KB/row); =REBEL_DATA_IN= loads one (fixed row%50 heldout
  split ≤8192 rows, comparable across configs; target-variance baseline
  printed); =episodes=0= = offline mode — pure SGD epochs on the loaded
  data, so architecture screens run at train-speed, not resample-speed.
  New knobs: =REBEL_HIDDEN=, =REBEL_LAYERS= (2 = ckpt-compatible l1/l2/l3),
  =REBEL_SGD_STEPS=, =REBEL_BATCH=, =REBEL_LR=, =REBEL_LR_FINAL= (linear
  decay), =REBEL_REPLAY_CAP=, =REBEL_CKPT=, =REBEL_SEED=. Trap closed:
  torch::load REPLACES parameter tensors, silently loading a
  foreign-architecture checkpoint — explicit shape check aborts instead.
- paper-fidelity audit (2026-07-06, vs ReBeL arXiv:2007.13544 + DeepStack
  arXiv:1701.01724, both re-read): VERIFIED faithful — ε=25% with one
  randomly chosen explorer (paper: "one agent samples random actions with
  probability ε... we set ε=25%"); t*~unif iteration sampling; CFR-AVG
  next-root beliefs under the average policy (paper: "In CFR-AVG and FP
  β'_r = β_z^π̄t" — sample_leaf matches); per-infostate values normalized
  by opponent mass; raw 1326 beliefs as net input (ReBeL; DeepStack's
  1000-bucket encoding is the fallback if features cap out); pot-unit
  values (DeepStack: "fractions of the pot size"; ReBeL doesn't specify).
  GAPS worth adopting, all paper-grounded: (1) loss — both papers use
  HUBER, we use MSE; (2) DeepStack's zero-sum output correction (subtract
  half the range-weighted value-sum violation from each player's values);
  (3) range generation — DeepStack's R(S,p) recursion splits by HAND
  STRENGTH ordering, ours splits a random shuffle: mass sparsity matches,
  strength correlation is missing (real CFR ranges are polarized — likely
  part of the residual net-vs-solver distribution gap); (4) ReBeL's buffer
  is CIRCULAR 12M + uniform sampling (recency window) — our
  reservoir-over-history is equivalent for stationary river targets but
  wrong for train_turn, where the query distribution tracks the net;
  (5) paper net specs: ReBeL 6×1536 GeLU+LayerNorm, Huber, Adam 3e-4,
  batch 1024, lr halved / 800 epochs; DeepStack 7×500 PReLU — ours
  (2×1024 ReLU, MSE, 1e-3, 128) is smaller than both; the capacity screen
  should top out at the ReBeL spec (net builder needs GeLU+LayerNorm).
  Known deliberate deviations (documented, staged back toward the paper):
  DeepStack-style synthetic situations for endgame pretraining (ReBeL
  itself is "purely from self play without any additional heuristics
  guiding which PBSs are sampled" — train_turn is the ReBeL-proper phase);
  exact river solves as targets instead of the paper's (1/T)Σ_t v^πt
  iterate average (ground truth on rivers; use the iterate average when
  bootstrapped turn-root targets arrive for the flop net); no policy-net
  warm start (paper Appendix J, optional); unsafe resolving (gadget
  pending). Non-issues: the paper's "bet occurred this round" input flag
  is constant-false at street roots (our only query points); DeepStack's
  heavy-tailed pot-interval sampling vs our prefix-induced pots — worth a
  histogram check of big-pot coverage, not a correctness gap.
- audit adoptions (2026-07-06, same day): Huber loss is now the DEFAULT
  (=REBEL_HUBER_DELTA=, delta=1.0 pot units; 0 = plain MSE. vloss prints
  Huber — reads ~half of MSE in the quadratic regime; heldout stays MSE
  for cross-run comparability). R(S,p) is now STRENGTH-ORDERED (exact
  combo_rank on the river, expected rank over river cards on the turn;
  invalid combos excluded from the recursion). Consequences: the target
  distribution changed — REGENERATE data and re-baseline heldout (the
  target-var line at data load is the new "predict the mean" baseline;
  the old run's 1.05e-2 is not comparable), and probe situations' ranges
  changed (new exact refs — ~9.7 bb on probe seed 12345). =REBEL_GELU_LN=1=
  adds LayerNorm+GeLU hiddens for the ReBeL-spec screen arm (6×1536) —
  new parameters, needs a fresh REBEL_CKPT (the shape guard catches it).
- audit adoptions, part 2 (2026-07-06): DeepStack's ZERO-SUM outer step is
  now a differentiable layer in the net (default ON, =REBEL_ZERO_SUM=0=
  off): in our value convention the identity is
  Σ r0·m0·v0 + Σ r1·m1·v1 = 0 (m = opponent compatible mass via
  inclusion-exclusion, computed from the input ranges with a fixed
  combo→card matrix — two matmuls); the violation, split evenly (s/2Z per
  entry, Z = Σ r·m identical for both players), is subtracted from every
  value. VALIDATED from outside: exact solver targets satisfy the identity
  to mean 7.8e-10 / max 1.2e-9 pot units (load_dataset now prints this
  residual — if it ever reads large, the correction formula and the
  targets disagree and training would fight itself). The combo→card
  matrix is deliberately NOT a registered buffer (old ckpts keep loading);
  =to(Device)= is overridden to move it. Replay buffer: ReBeL's CIRCULAR
  (overwrite-oldest recency window) is now the default for =train_turn=
  (=REBEL_CIRCULAR= overrides; auto = circular for turn, reservoir for
  river — river targets are stationary so uniform-over-history keeps more
  diversity). Pot-coverage check CLOSED: =[gen]=/=[data]= pot/stack
  histograms added; measured 13/20/20/15/13/19% across
  {<5,5-10,10-20,20-40,40-70,70+}% bins — the prefix-induced distribution
  already has DeepStack-grade heavy-tail coverage, no sampling change
  needed. Note: strength-polarized ranges raise target variance (~3.6e-1
  heldout target var vs old distribution) — judge heldout against the
  printed baseline, not old runs. Still pending, tied to the flop-net
  phase: iterate-averaged root targets ((1/T)Σ_t v^πt) — not applicable
  until turn-root values become training targets (today all targets are
  exact river solves); policy-net warm start; safe-resolving gadget.
- =hunl_value=: PBS value net (street/board/pot + both 1326-ranges →
  per-combo values both players) + Algorithm-1 endgame self-play
  (=rebel_hunl train_turn=): synthetic turn roots (random boards/pots via
  env prefixes, random ranges), turn solves with NET leaves, t*-sampled
  leaf continuation with ε-exploration, EXACT river solves as grounded
  targets. Smoke run (6 epochs × 12 episodes, CPU): pipeline executes
  end-to-end; net-leaf probe-turn expl 0.66 → 0.47 bb vs 0.275 bb
  exact-leaf reference on the same root. Smoke-scale caveats: replay ~50
  samples → vloss/probe are memorization numbers; switch target
  normalization from stack to POT (paper) before real runs.

Remaining for a playing agent (box-scale work): long =train_turn= runs on
GPU (net + batched leaf queries; flat-tensor solver for full-action
trees); then the flop net bootstrapped through turn solves the same way;
PBS tracking during live play (adapt LBR's Belief); LBR evaluation harness
for the search agent; safe-resolving gadget.

* HUNL port map (stage 2)

- Beliefs/ranges: LBR's =Belief= (src/lbr.cpp) already Bayes-tracks
  1326-combo ranges — reuse for the PBS tracker.
- Subgame solver: this solver's structure, vectorized over 1326 hands
  on GPU tensors; terminal fold eval O(n) via the sum-minus-blockers
  trick; showdown eval O(n·52) via rank-sort sweep (Game/Utility
  RangeEquity/HandStrength kernels are the starting point).
- Depth limit: current street; river subgames solve to terminal
  (exact, no net) — mirrors round-2 here.
- Leaf value net: public state (board one-hots + pot/stack features +
  bet history) + both ranges → per-hand values both players.
  The ESCHER privileged critic marginalized over a range is a
  reasonable warm-start / baseline comparator.
- Bet abstraction: the env's existing 14-action config.
- Self-play at scale: batch leaf-net queries per iteration (already
  the structure here), episodes parallel across CPU workers, net on
  GPU.
- Open items, in priority order: safe-resolving gadget for unreached
  subgames; policy-net warm start (paper §5, optional); longer
  training + larger nets; exploitability judged by LBR on HUNL.
